# 第二模块代码入口

历史严格复核入口是 `strict_ablation.py`，独立审核和报告入口是 `summarize_strict.py`。历史v1结果已完成，但M2未验收；此后代码加入过安全回退，源码签名与历史结果不再相同，不能将当前源码直接用于重跑/认证历史v1目录。

```bash
python 模块研发/m2/repair_diagnostics.py --family source_lodo --workers 3 --out 模块研发/results/m2_source_lodo_new_run
python 模块研发/m2/audit_repair_screen.py 模块研发/results/m2_source_lodo_new_run
```

运行协议为 `protocol_strict_ablation_v1.json`，结果目录为 `模块研发/results/m2_strict_ablation_v1/`。

四组：冻结原始 GPR、GPR+M1、GPR+M2、GPR+M1+M2。M2在每个外层折内独立选择参数，所有分支共用原来至多1000个源标签，目标只使用前10个支持标签。5种随机种子改变源电芯交叉拟合划分；冻结基线不是5次独立训练。

`global_domain_blend.py` 的历史方法已停用。`m2_global_domain_blend_v1` 的验收结论撤回，原数值保留并标记 INVALIDATED。原因包括跨外层折调权泄漏、校准器超出源标签预算和旧物理预测偏移遗漏。`audit_legacy.py` 可重算其审计证据。

其他旧探索脚本/结果保留作历史记录。它们不是四组严格消融结果；不得把旧校准器或旧预测缓存直接带入新实验。

## 当前继续修正

`repair_diagnostics.py`支持组件拆分、支持留一门控、查询方差门控、源内留工况选择、源误差学习、后验更新和因果滤波等开发筛查，具体family选项见`--help`，默认seed=0。复用的是严格v1预算内的源交叉拟合校准器；source_lodo变体alpha固定为0，校准器不参与修正。源误差学习可通过`--inner-cache`复用已核对的源工况留出预测。

筛查输出必须使用独立目录，保存候选和选择以及逐电池预测；新运行同时保存源码快照。`audit_repair_screen.py`检查覆盖/保存指标，重放源LODO或原验证集选择，并在残差头实验中重拟合误差模型，不等同于全量独立重训练认证。19项测试覆盖已发现的兼容及推理边界问题。

详见`心路历程/第二模块继续修正记录_20260908.md`。目前尚无M2最终验收通过结论。

## 已封存的数值候选

`模块研发/results/m2_reference_candidate_v1/`包含参考信号风险门控HI版的42个适配器、源码快照、四组表及成对不确定性。`reference_adapter.ReferenceRiskAdapter.predict(cell)`只暴露前10个支持标签，已逐电芯重放并检查未来前缀不变性。

本候选四组均值门槛通过，但XJTU独立增益很小且统计区间跨零，部分Tongji互补区间也跨零，状态仍为需要独立确认的研发候选。当前22项回归测试通过；不能据此声称新颖性或稳定显著提升已获证实。

后续已封存`m2_reference_candidate_v2`（参考去冗余/PCA8）和`m2_reference_candidate_v3`（另加24次源风险路由汇总）。两者均四组均值过线，v2偏重XJTU增益，v3改善风险权重敏感性和MATR/Tongji组合。`reference_sensitivity.py`只扰动源风险估计，不重新训练GP/PCA；它与标准多种子训练不同。v3仍有2/10次抽样的XJTU独立门槛未过，研发目标仍在进行。

当前26项回归测试通过，主入口可使用`--family reference_metric`或`--family reference_bagged`；封存通过`freeze_reference_candidate.py --variant ...`明确指定候选。旧适配器保留自己的参数表、特征映射和风险设定。

## 覆盖保护、稳定性与三指标修正（最新）

已封存覆盖保护v4及固定源覆盖分位点、128次内部风险汇总v5。四组均值均过线，但同样10组源风险抽样中，独立/叠加/互补通过次数分别为v4的10/10、10/10、9/10与v5的10/10、10/10、5/10，均不能宣布修正完成。v5主要余留MATR的P95互补退步；覆盖保护也牺牲了早期版本部分Tongji收益。源工况多数一致性版本失败，未封存为候选。

`audit_reference_sensitivity.py RESULT_DIR`核对敏感性逐电芯覆盖、逐域汇总、门槛和两父模型配对抽样清单，输出`aggregation_audit.json`；该审核不重放预测、不重训GP，也不是独立最终确认。

新增`--family reference_minimax`：将源风险选路和折内验证选择改为三指标相对父模型风险比的最坏项最小化，仍固定源覆盖分位点0.95及128次风险汇总。结果目录`m2_reference_minimax_screen_v1`，是否完成查protocol.json；尚无成功结论。旧模型缺省仍用MAE优先的字典序，保存的新模型带risk_objective属性，不改写旧模型。

当前32项回归测试通过。v4/v5的42个适配器均完成365电芯两组重放与查询标签/未来前缀检查；保留数据未启用，M3未启动。完整过程与失败数值见心路历程继续修正记录。

三指标版筛查现已完成并审核，封存为`m2_reference_candidate_v6`，四组均值门槛通过，42个模型完整重放及查询标签/未来前缀检查通过。相同10组风险抽样中独立/叠加/互补为10/10、10/10、8/10；第105组MATR尾部误差、第106组XJTU三指标仍有互补退步。尚不能作为已通过全部稳健性要求的M2。

当前39项测试通过（上述6个test文件加`test_audit_reference_sensitivity.py`），包含旧路由排序兼容、三指标风险排序及审核器拒绝错误记录。v6的完整不确定性见candidate.json，抽样失败项见`m2_reference_candidate_v6_risk_sensitivity/aggregation_audit.json`。不存在“全部数据集全部电芯/工况都改善”的结论。

## 原始GPR参照与模块冲突修正

`--family reference_raw`新增冻结原始GPR骨干参照，源OOF专家可混合父模型与原始GPR及对应支持残差；相同骨干去重，独立M2在365电芯预测与v6精确一致。v7已封存、重放，10组敏感性独立/叠加/互补10/10、10/10、6/10，完整组合平均指标也不及v6，不采用为替代版。

`--family reference_raw_backbone`进一步把覆盖收缩基准改为所选骨干混合，只收缩残差/物理迁移部分。已完成全折筛查，XJTU/Tongji组合三项均值优于v6，MATR略退但仍优于两个单模块；独立M2仍逐点不变。结果目录`m2_reference_raw_backbone_screen_v1`，审核/封存/敏感性按实际文件状态判断，不预先宣称通过。

当前44项测试通过。新增源码快照保留敏感性运行脚本；原始参照源标签配对、冻结模型哈希和验证参照预测重算进入审核；适配器保存额外原始GPR对象及模式，旧适配器缺省分支保持兼容。原始GPR参照会增加完整模型的存储/推理开销，不是额外源标签预算，也不应隐瞒额外骨干参与。

骨干/迁移收缩分离版现已审核并封存为`m2_reference_candidate_v8`，42个适配器完整重放及标签/前缀检查通过。相同10组敏感性独立/叠加/互补为10/10、10/10、9/10，仅剩第102组MATR的P95互补退步0.0302254个百分点。v7、v8的3650条独立M2敏感性结果与v6逐项完全一致。仍不是完整验收通过，不丢弃旧候选；下一步可检查两组不同父模型归一化带来的风险尺度差异，尚未实现。

## 共同尺度失败与稀疏风险估计修正

共同风险尺度`--family reference_common_scale`已实现并完成21折审核，但完整模型九项均值均不及v8，不采用、不封存为v9。v8另完成种子1001–1030的30组新风险抽样，独立/叠加/互补为30/30、30/30、25/30，五次均有MATR P95互补退步，一次兼有RMSE退步。不能把新抽样说成新数据或独立最终测试。

`reference_sensitivity.py --seed-start 1001 --seed-count 30`可明确指定新风险抽样，默认仍101–110，输出保存实际种子和脚本副本。审核按实际种子数检查全覆盖。

新增`--family reference_position_risk`：仅用原预算源查询误差在记录位置网格上线性插值来估计源风险。预算外标签不参与，插值不是新增观测；实际验证和评分保持原完整查询指标。该方案沿用v8结构，不叠加共同尺度失败方案。全21折筛查及常规审核完成，正在封存为v9；源风险重建审核另新增预算外标签掩蔽检查。当前49项测试通过，最终是否有效须看模型重放与敏感性实际结果。

v9现已封存并完成全部电芯预测/标签/前缀重放，42份风险表在预算外源标签遮蔽后也能一致重建。原10组敏感性10/10、10/10、9/10；另30组30/30、30/30、26/30，仍有MATR P95互补退步。额外对照不变的v8独立M2时原10组仅7/10，新30组26/30，不能依靠新版独立组变化宣布更强互补。v9保留为研发候选，不直接替换v8；49项测试通过，所有本轮任务已结束，继续研发目标未完成。

## 查询状态路线（最新）

`diagnose_error_positions.py`输出v8/v9误差位置及源预算分段描述。回顾用的完整查询长度分段绝不作为模型输入。固定v8的MATR大误差约89%在最后三分之一记录，后段MAE互补略退，但该现象不是因果证明。

`--family reference_state`增加`state_risk.py`：源预算内条件风险以当前预测退化量为代理，源q20/q50/q80中心及带宽固定，逐查询插值路由结果。已封存v10、完整重放与预算遮蔽审核通过，但原10组独立/叠加/互补10/10、10/10、6/10，不替换v8，不扩新30组。

`--family reference_state_physical`改用同一个冻结物理GPR的当前预测作代理。虽源排序相关性更好，全折MATR完整P95=5.3485仍劣于独立M2的5.3134，均值互补失败，不封存v11，不继续抽样。结果及42次风险/标签边界重放已审核。

当前56项测试通过，新增状态代理配置兼容、源预算边界、逐查询前缀、状态风险抽样行对齐和诊断等权汇总测试。新依赖随筛查/冻结模型保存源码快照。两条状态路线均未修好剩余MATR问题，v8也未因此获得最终验收；所有本轮任务已结束，保留数据和M3未启用。

### 嵌套源域信号判断诊断

`nested_signal_probe.py --out <new_directory> --workers 3`运行MATR源预算内试验；`audit_nested_signal_probe.py <directory>`重放审核。模型训练误差用同时排除元验证域和被预测域的GP产生；原冻结配置不重新嵌套选择。4种方法为固定增益prior、支持段HI reference、当前HI变化delta、拼接dual，固定RBF核岭回归参数，验证标签不用于拟合。不是完整M2或最终测试。

`m2_nested_signal_probe_v1`完成且审核PASS（36个GP、1440次预测、96次判断器重放）。信号方案平均选择MAE均劣于prior，不接入M2、不生成v11。总62项测试通过。细节与限制见心路历程继续修正记录；结果不能证明所有信号方案无效，下一步先检查完整专家库的可达收益与风险选择误差。

`diagnose_expert_capacity.py --out <new_directory>`核对源预算专家风险表，报告电芯/逐点理想选择及凸组合下界，全部理想值用答案、不可部署。`nested_expert_probe.py --out <new_directory>`复用已审核双排除GP，重建仅前K标签支持残差，按元训练风险选择固定专家后评分留出源域。不是v8路由或外层指标。对应v1结果均完成：扩大专家库的固定选路劣于两分支参照，暂不采用；48次选择和960条评分重放一致。新增3项测试，总65项通过（64项回归及1项新增单测）。下一步检查不可靠修正的保守收缩，保留原v8与全部失败证据。

`nested_shrink_probe.py --out <new_directory>`增加固定256袋风险重抽样、三指标q10收益收缩及训练域一致性收缩；`audit_nested_shrink_probe.py <directory>`从该结果的源码快照重放，兼容v1/v2。收缩是启发式，非选择校正置信保证，GP不随风险抽样重训。v1在M1分支退步；v2域一致性版只有0.0018/0.0056个百分点源诊断MAE收益，不替换v8。审核分别24次选择、1920/2400条评分PASS，69项测试通过。下一步检查风险组成变化与跨开发数据迁移；尚未得到最终M2修复版。

`nested_shrink_sensitivity.py --out <new_directory>`固定2001–2010风险种子，比较同次抽样及原固定两种简单参照；`audit_nested_shrink_sensitivity.py <directory>`重放抽样、选择、指标和门槛。v1结果严格改善仅3/10、4/10，固定参照下平均指标退步，不采用、不扩跨数据集。审核240次选择、120组配对、14400条评分PASS；71项回归测试通过。GP未重训，不能称为独立最终确认；下一步先分解简单参照自身不稳与复杂修正的影响。

`nested_binary_stability.py --out <new_directory>`分解先前指标差，并试验256袋两分支选择频率平均；`audit_nested_binary_stability.py <directory>`重放264次权重、5280条评分及汇总门槛。v1未扰动只改善M1分支，风险扰动相对固定参照0/10全部指标改善，不采用。差值分解不是因果归因，二分支探针不是v8外层成绩。全73项测试通过；接下来检查既有残差路线与尚未验证的预测能力改进，v8保持且未获最终验收。

`nested_signal_residual.py --out <new_directory>`试验固定RBF/零参考锚定RBF的当前HI变化→有符号残差；`audit_nested_signal_residual.py <directory>`预算外标签遮蔽后48头重拟合、1440条评分重放。v1两方案两分支三指标全退步，不采用；75项测试通过。锚定核仅保证支持均值特征处修正为0。后续先核查嵌套1域/2域基础GP的误差目标变化，仍必须保持源工况隔离，v8不变。

`diagnose_nested_training_shift.py --out <new_directory>`诊断匹配源查询上的单域/双域GP残差变化；`matched_residual_probe.py --out <new_directory>`保持残差头，改用排除评分域的单源域GP集成作诊断父预测。各v1结果1440条唯一记录，残差变化明显但对照MAE仍退步，不采用、不替换冻结控制。只做输入/头哈希/记录核对，未独立全重放，非纯样本量因果实验。77项回归测试通过，暂不扩大当前HI残差路线。

`diagnose_support_envelope.py --out <new_directory>`检查固定支持最大值上界，v1仅物理分支三数据集三指标均值全改善；GPR/M1统一限幅伤害XJTU，经验上界不保证逐点无害。3435条记录/汇总核对，79项测试通过。下一步拟只接入v8物理专家并同步源风险/推理，尚未实现或外层验证；不能视为M2修复成功。

`--family reference_physical_cap`现已实现，源风险/推理仅内部physical限幅，返回记录保持原物理控制字段。screen_v1曾触发历史控制一致性断言，已标无效；修复后screen_v2完成21折并审核42次风险/选择及42次预算外标签屏蔽重建。80项测试通过。完整模型MATR/Tongji改善但XJTU退于v8；候选v11封存正在进行，以request.json/实时进程为准，尚未稳定性验收、不替换v8。

v11现已封存重放PASS；10组风险独立/叠加/互补10/10、10/10、7/10，不替换v8、不扩30组。`compare_reference_versions.py --old <risk_dir> --new <risk_dir> --out <new_dir>`核对源ID抽样配对并比较固定旧参照；v11完整胜v8完整0/10、胜v8独立M2为7/10。结果均保存，81项测试通过。所有本轮进程结束，下一步诊断输出限幅与风险重建选路的各自影响。

`diagnose_cap_routing.py --out <new_dir>`完成v8→仅限幅→更新风险旧设置→v11路径诊断。XJTU直接限幅已伤害MAE，不能仅恢复旧设置；中间混合头只供诊断不可部署。2920条评分，旧/新端点逐点与保存预测一致，全82项测试通过。下一候选拟用原始GPR当前预测放宽物理支持上界，尚未实现，不按数据集开关。

`--family reference_physical_rawcap`现已实现，physical_cap字符串`raw_supported`保存原始GPR支持的上界，旧True/False兼容。screen_v1完成21折，完整XJTU/MATR三指标均劣于v11、Tongji不变，不采用、不封存v12。42风险/选择及42标签屏蔽重建审核PASS，84项测试通过；未运行该版风险抽样。下一步仅先核对保留原物理并增加限幅可选专家的源证据，避免盲扩专家库。
